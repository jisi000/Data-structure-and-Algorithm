调度场算法

```python
s=list(input().split())
num=[]
sym=[]
seq={"+":1,'-':1,'*':2,'/':2}
def cal():
    a=num.pop()
    b=num.pop()
    sy=sym.pop()
    if sy=='+':
        num.append(b+a)
…    else:
        while sym and seq[sym[-1]]>=seq[ch]:
            cal()
        sym.append(ch)
while sym:
    cal()
print(f'{num[0]:.2f}')
```

