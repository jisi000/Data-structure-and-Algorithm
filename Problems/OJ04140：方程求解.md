根据牛顿迭代法公式
$$
x_{n+1} = x_n - \frac{f'(x_n)}{f''(x_n)} 
$$
在判断是否结束迭代时，使用容差比较，这里设置容差为10^(-6)

```python
x0=5
while abs(x0**3-5*(x0**2)+10*x0-80)>1E-6:
    x1=(2*(x0**3)-5*(x0**2)+80)/(3*(x0**2)-10*x0+10)
    x0=x1
print(f'{x0:.9f}')
```

