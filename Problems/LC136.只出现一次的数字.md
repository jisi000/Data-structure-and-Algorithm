两个相同的数进行异或运算得0，对数组中的全部数字连续异或运算，最终留下的就是只出现一次的数

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        ans=0
        for num in nums:
            ans^=num
        return ansclass Solution:
    def singleNumber(self, nums: List[int]) -> int:
        ans=0
        for num in nums:
            ans^=num
        return ans
```

