KMP

```python
import sys
def compute(st):
    n=len(s)
    lps=[0]*n
    length=0
    for i in range(1,n):
        while length>0 and st[length]!=st[i]:
            length=lps[length-1]
        if st[length]==st[i]:
            length+=1
        lps[i]=length
    p=n-lps[n-1]
    if n%p==0 and n!=p:
        return n//p
    else:
        return 1
data=list(sys.stdin.read().split())
for s in data[:-1]:
    print(compute(s))
```

