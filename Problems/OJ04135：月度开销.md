二分查找

```python
N,M=map(int,input().split())
spend=[]
for _ in range(N):
    spend.append(int(input()))
def check(x):
    re,c=0,1
    for s in spend:
        if re+s>mid:
            c+=1
            re=s
        else:
            re+=s
    return c<=M
hi=sum(spend)+1
lo=max(spend)
while lo<hi:
    mid=(lo+hi)//2
    if check(mid):
        hi=mid
    else:
        lo=mid+1
print(hi)
```

